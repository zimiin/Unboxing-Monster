import {
  StyleSheet,
} from 'react-native'

const globalStyles = StyleSheet.create({
  badge: {
    position: 'absolute',
    top: -10,
    right: -10,
    zIndex: 100,
  }
})

export default globalStyles