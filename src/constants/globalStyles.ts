import {
  StyleSheet,
} from 'react-native'

const globalStyles = StyleSheet.create({
  badge: {
    position: 'absolute',
    top: -8,
    right: -9,
    zIndex: 100,
  }
})

export default globalStyles